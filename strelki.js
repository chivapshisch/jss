/*let getter = (age, nam) => {
    let message = 'my age: ' + age + ' my name: ' + nam;
    return message;
}
    console.log(getter(22, 'HIu'));

let namer = (tutor, address) => {
    let res = tutor + address;
    console.log(res);
} 

setTimeout(namer, 3000, 'John', 'st Zeleznay');
setInterval(namer, 500, 'vaha', 'st Grozg');*/

/*function clock(now_time){
    for (;now_time < 10;){
        console.log(now_time);
        now_time ++;
    } 
}
setInterval(clock, 1000, 0);*/
